# The stem you're working from

- Source: *Hot Pants (She Got to Use What She Got to Get What She Wants)*, James Brown, 1971 — pulled as vocal-only via the `w09-hot-pants-vocal-stem` recipe, LALAL backend (`bib:stemforge_repo`).
- The recipe carries a `determinism_note`: LALAL is **non-deterministic**. Re-running produces a different isolation. This run is the source-of-truth; the bounce on disk is canonical. Do not re-render to "get a cleaner version."
- Expect bleed: reverb tails, a ghost of the horn stab, consonant smearing around plosives. This is not a flaw. It is texture.
- `whosampled_db` lists 600+ uses of the Hot Pants breakdown (`bib:whosampled_db`). You are not clearing it — you are transforming it. Prefuse's ethic applies (`bib:prefuse_73_xlr8r`).
- Before you touch Simpler: commit the stem to a frozen audio track and bounce. The chopping happens on the bounce, not the original.
