The assignment. [pause 250ms] Thirty to sixty minutes. If you are past ninety, you are over-thinking it.

Take any drum loop. Yours — something you have been sitting on. Or the Hallogallo drums stem from the stemforge recipe — `w04-hallogallo-warp-ab`. Or a free break off your hard drive. Or something from Looperman. Does not matter. One loop, bounced dry to a clean WAV. [pause 200ms]

Render it three times. [pause 150ms]

**Render one — Complex Pro.** Formants one hundred. Envelope one-twenty-eight. Export as `01_complex_pro.wav`.

**Render two — Beats mode.** Preserve Transients. Transient Loop Mode Off. Transient Envelope one hundred. Export as `02_beats.wav`.

**Render three — Re-Pitch, unchanged pitch.** Just Re-Pitch as the mode, no transposition. Export as `03_repitch_0st.wav`. [pause 100ms] Then do a second Re-Pitch render, pitched up five semitones. Export as `04_chipmunk_soul_bank.wav` — note the filename, we will need it by name in Week nine.

[pause 300ms] Now the written part. Three sentences. One per mode — Complex Pro, Beats, Re-Pitch. Each sentence must answer two things. What happened to the *transient envelope* — sharper, softer, unchanged. And what happened to the *formant character* — vowel content preserved, shifted, or irrelevant. Do not over-write this. Three sentences. [pause 200ms]

Deliverable — four audio files plus a text note, zipped as `w04-<yourname>-warp-ab.zip`, uploaded by Sunday.

The chipmunk render — bank it. Put it in a folder called `w09-bank` in your project root. Week nine, when we spend a full session on vocal chopping, you will drag it back out. [pause 200ms]

That's the week. Three modes, three sentences, one banked render. [pause 400ms] Go.
