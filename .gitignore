# Regenerable / host-specific — excluded from git
build/audio/narration/
build/audio/stemforge-renders/
build/glossary.md

# Local + Python noise
node_modules/
.venv/
__pycache__/
*.pyc
.DS_Store

# PDFs fetched from external sources (licensed content — cite, don't mirror)
references/pdfs/*.pdf
references/pdfs/*.html
references/pdfs/max_for_live_readmes/
references/transcripts/*.md
!references/transcripts/README.md

# Local screenshot inbox
_screenshots-inbox/

# Committed to repo:
#   lessons/                     — all source content + slide bodies
#   references/                  — glossary.json, bibliography.json, songs.json, images.json
#   build/audio/episodes/        — 12 MP3 podcast episodes (~434 MB)
#   build/html/                  — 12 rendered HTML decks + index + glossary
#   build/pdf/idm_glossary.pdf   — reMarkable-ready glossary
#   build/diagram_sanity.*       — QA reports
#   build/images_fetch_status.json
