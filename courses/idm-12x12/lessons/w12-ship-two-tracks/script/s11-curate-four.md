Before you touch a limiter this week — do this. [pause 300ms]

Open a new Live set. Call it `w12_curation.als`. Drag every bounce from Weeks one through eleven into the arrangement view as audio clips, in order. Sequentially. [pause 200ms] Level-match them with Utility, the same way you level-matched references against your master three slides ago. Set each Utility so the tracks read the same integrated LUFS on Youlean. You do not want the quieter Week-two bounce to sound worse than the louder Week-seven bounce by accident.

[pause 500ms]

Listen. Whole stack. Beginning to end. One pass. On headphones, preferably on the K371 for the low-end translation. [pause 200ms] Do not stop to fix anything. Do not re-open any of the source sessions. This is a listening pass, not a re-working pass.

[pause 400ms]

On paper — pen and paper, not a text file yet — write one sentence per track. Two columns. Left column: what you *meant* this track to sound like. Right column: what it *actually* sounds like now, twelve weeks later, with distance. [pause 300ms] The gap between the two columns is a useful document.

[pause 500ms]

Then circle four tracks. The four that feel most like *you*. Not necessarily the best-produced. Not the most ambitious. The ones where your defaults — the ones we named in the previous slide — showed up without being forced. The ones that sound recognizable as *yours* if you played them back-to-back with somebody else's IDM record.

[pause 400ms]

Those four are the EP candidates for Year Two. You are *not* going to master all four this week. You will master *two* — the two closest to finished. The other two go into a folder called `year-two-candidates` and wait. Year Two's work is refining those four into a record. This week's work is shipping two.

[pause 400ms]

The deliverable for this slide is a single text file — `w12 underscore yourname underscore curation dot t-x-t` — with all twelve sketches listed, each with the two-column description. Star the four candidates. Circle the two for this week's upload.

[pause 300ms]

~Ship the list first. The bounces come next.~
