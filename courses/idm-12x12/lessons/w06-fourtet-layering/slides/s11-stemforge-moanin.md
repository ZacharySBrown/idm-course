## Stemforge glitch render on Moanin' — what to listen for

Recipe: `w06-moanin-glitch-layers`. Source: "Moanin'" (Art Blakey & The Jazz Messengers, 1958). Pipeline: `stemforge split {input} --pipeline glitch`. `bib:stemforge_repo`.

Renders in `build/audio/stemforge-renders/w06/`:
- `moanin_dry.wav` — unprocessed split reference.
- `moanin_glitch.wav` — glitch pipeline wet. Transient re-slicing, pitched ghost tails, occasional reverse stabs.

Listen for:
- **Sub-audible ghost tails** — quiet, band-limited, contextual. Exactly the Hebden subliminal-layer material.
- **Pipeline artifacts as texture** — the detritus is source, not bug. The "dirty intake" posture.

For the exercise: keep `moanin_glitch` on a return or a muted parallel. The kick stack goes on its own group, tuned to the Moanin' bass walk (F minor — root and fifth).

Do not mix the stem up to equal the stack. It is a subliminal bed.
