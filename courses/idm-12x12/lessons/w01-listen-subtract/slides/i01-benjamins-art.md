# Benjamins — album art, in context

The reference track lives with a cover. Look at it once, then ignore it for twelve weeks — the audio is the job, the artwork is the label.

`benjamins.wav` is loaded already in stemforge. The four-stem split was produced by the <term key="demucs">Demucs</term> pipeline via `default --no-slice`. <Citation bib="stemforge_repo" />

Hang the cover somewhere in the session as a muted clip marker if it helps you remember which track you're A/B-ing against. That is all the art needs to do this week.
