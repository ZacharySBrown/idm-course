Step two. Beats — into a stutter-glitch.

[pause 400ms]

Same two seconds. Mode to Beats. Preserve to a sixteenth — deliberately gridding it instead of following the real transients of a held vowel. Transient Loop Mode to Loop Forward, so each slice freezes into a loop. Transient Envelope to a hundred, for hard edges. Then drop the tempo to half. Here is the smooth "ah" turned into a chopped, stuttering machine.

[cue: walk-beats-stutter]

A sung vowel has almost no transients — it is one long sustain — so gridding Beats to a sixteenth tells the engine to cut where there is nothing to cut, and Loop Forward freezes each fragment. The voice becomes a rhythm it never performed. That is breakage one.
