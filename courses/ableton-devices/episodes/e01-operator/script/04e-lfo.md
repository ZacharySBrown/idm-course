Now the *added* column — the things Chowning's four operators couldn't do on their own. Start with the LFO, because Operator's is secretly a fifth operator. The LFO has a Range chooser with three settings: Low, Hi, and Hi audio. In Hi audio, the LFO reaches roughly twelve kilohertz. Which means it functions as a fifth audio-rate operator.

[pause 500ms]

The killer Operator trick: route the audio-rate LFO simultaneously to the pitch destinations of A, B, C, and D. You have just synthesized a routing the eleven algorithms don't otherwise allow — one modulator hitting four carriers in parallel from outside the algorithm matrix.

[pause 400ms]

If you also turn R-less-than-Key to one hundred percent, the LFO frequency doubles per octave. It tracks pitch like a real oscillator. Combine that with the LFO's own ADSR envelope on its amount — most users don't realize the LFO has an envelope — and you get a fully-modulated audio-rate fifth operator.

[pause 500ms]

This is how you escape the eleven-algorithm prison.
