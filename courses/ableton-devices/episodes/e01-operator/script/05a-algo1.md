We are going to build a patch live. The target is an Aphex-style inharmonic bell-pluck. Eight steps. Roughly six minutes. By the end you will have something you could drop into a Drukqs-era track without anyone flinching.

[pause 500ms]

Step one. Open Operator. Set the algorithm to one — the linear stack. D modulating C, modulating B, modulating A. Set every operator's wave to Sine. Default Coarse, default Level, no envelope tweaks yet.

[pause 500ms]

Listen.
