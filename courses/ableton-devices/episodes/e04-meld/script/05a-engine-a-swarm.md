Still on stop three — now build it. Seven steps, about five minutes. We're building one patch — call it Two-Hands — a bi-timbral, MPE-played texture-lead that proves the whole thesis: two synths, one note, ten fingers. One decision at a time, because two macros only mean something one decision at a time.

[pause 500ms]

Step one of seven. Engine A. Set the oscillator type to Swarm Saw — one of the scale-aware ones. Push Motion to around thirty, Spacing to around forty, so the swarm is wide but not unhinged. Drop a gentle low-pass over it. Make sure the Live Scale is set, so the spread stays in key. Hold a chord. Listen.

[cue: meld-twohands-step1]

That's the foundation — a wide, in-key saw body, anthemic, partials snapped to the scale. Nothing else is on yet. One engine, one sound. Now we give it a second.
